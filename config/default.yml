git:
  diff_mode: "head"
  exclude_patterns:
    - "*.lock"
    - "*.min.js"
    - "vendor/**"

agent:
  name: "gemini"
  timeout_seconds: 300

decision:
  block_on_agent_failure: false
  min_quality_score: 9
